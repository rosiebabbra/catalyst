def match(user_a, user_b):
    """
    Any two given users who are located within x miles of one another 
    and have selected the same interest constitutes a `match`.
    This function should be run for user IDs who meet the following criteria:
    - Same location
    - Same interest

    Args: 
        user_a (dict): information about a user `a`
        user_b (dict): information about a user `b`
    
    Returns (bool): whether two users are matches
    """

